[project]
name = "compute-as-a-teacher"
version = "0.1.0"
description = "Reproducible experiments for compute-as-a-teacher training"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
